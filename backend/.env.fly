# Fly.io Environment Variables Template
# Copy this file and set the actual values for your deployment

# JWT Configuration
JWT_SECRET=your-secure-jwt-secret-here
JWT_EXPIRES_IN=7d

# Google Cloud Platform (Optional - only if using GCP services)
GCP_PROJECT_ID=
GCP_REGION=us-central1
GCS_BUCKET_NAME=

# AI Configuration (Gemini API)
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-1.5-flash

# PayFast Configuration (South African payments)
PAYFAST_MERCHANT_ID=
PAYFAST_MERCHANT_KEY=
PAYFAST_PASSPHRASE=
PAYFAST_SANDBOX=true

# Paystack Configuration (African payments)
PAYSTACK_SECRET_KEY=
PAYSTACK_PUBLIC_KEY=

# Subscription Pricing (in cents)
STUDY_HELP_PRICE=3900
STANDARD_PRICE=9900
PREMIUM_PRICE=14900

# Trial Configuration
TRIAL_DURATION_DAYS=14
WELCOME_PREMIUM_DAYS=14

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info

# Frontend URL (for CORS)
FRONTEND_URL=https://caps360-frontend.fly.dev

# Note: Set secrets using flyctl:
# flyctl secrets set JWT_SECRET=your-secret --app caps360-backend
# flyctl secrets set GEMINI_API_KEY=your-key --app caps360-backend
