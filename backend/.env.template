# Server Configuration
PORT=8080
NODE_ENV=development
LOG_LEVEL=info

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=caps360
DB_SSL=false
# Use standard PostgreSQL connection string if preferred
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/caps360
# For Azure PostgreSQL Flexible Server, use the Azure-provided connection string:
# DATABASE_URL=postgresql://<user>:<password>@<azure-server>.postgres.database.azure.com:5432/<db>?sslmode=require

# --- Azure Integration ---
# Azure Key Vault: secrets can be injected as environment variables in App Service
# Application Insights: set APPINSIGHTS_INSTRUMENTATIONKEY=<your-key>
# Azure AD B2C: set AZURE_AD_B2C_CLIENT_ID, AZURE_AD_B2C_TENANT, AZURE_AD_B2C_CLIENT_SECRET, AZURE_AD_B2C_POLICY, etc.

# JWT Configuration
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# Supabase Configuration (Auth Only)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# AI Configuration (Gemini)
GEMINI_API_KEY=your-gemini-api-key
GEMINI_MODEL=gemini-1.5-flash

# Payments (PayFast)
PAYFAST_MERCHANT_ID=
PAYFAST_MERCHANT_KEY=
PAYFAST_PASSPHRASE=
PAYFAST_SANDBOX=true

# Payments (Paystack)
PAYSTACK_SECRET_KEY=
PAYSTACK_PUBLIC_KEY=

# Business Logic
STUDY_HELP_PRICE=3900
STANDARD_PRICE=9900
PREMIUM_PRICE=14900
TRIAL_DURATION_DAYS=14
WELCOME_PREMIUM_DAYS=14
